build:
  maxIssues: 0
  excludeCorrectable: false

config:
  validation: true
  warningsAsErrors: true
  checkExhaustiveness: false

processors:
  active: true

console-reports:
  active: true

output-reports:
  active: true

comments:
  active: true

style:
  active: true

empty-blocks:
  active: true

performance:
  active: true

potential-bugs:
  active: true
